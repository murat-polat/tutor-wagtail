mysql -u root --password="{{ MYSQL_ROOT_PASSWORD }}" --host "{{ MYSQL_HOST }}" --port {{ MYSQL_PORT }} -e 'CREATE DATABASE IF NOT EXISTS {{ WAGTAIL_MYSQL_DATABASE }};'
mysql -u root --password="{{ MYSQL_ROOT_PASSWORD }}" --host "{{ MYSQL_HOST }}" --port {{ MYSQL_PORT }} -e 'GRANT ALL ON {{ WAGTAIL_MYSQL_DATABASE }}.* TO "{{ WAGTAIL_MYSQL_USERNAME }}"@"%" IDENTIFIED BY "{{ WAGTAIL_MYSQL_PASSWORD }}";'